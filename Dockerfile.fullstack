# ===========================================
# DevAssist Pro - Unified Fullstack Container
# ===========================================
# Этот Dockerfile объединяет Frontend (React) и Backend (FastAPI) 
# в единый контейнер с Nginx в качестве reverse proxy

# ============= STAGE 1: Build Frontend =============
FROM node:18-alpine AS frontend-builder

WORKDIR /frontend-build

# Копируем package.json и устанавливаем зависимости
COPY frontend/package*.json ./
RUN npm ci --only=production --silent

# Копируем исходный код frontend
COPY frontend/ ./

# Устанавливаем переменные окружения для production build
ENV NODE_ENV=production
ENV REACT_APP_API_URL=/api
ENV REACT_APP_WEBSOCKET_URL=/ws
ENV GENERATE_SOURCEMAP=false
ENV INLINE_RUNTIME_CHUNK=false

# Собираем production build
RUN npm run build

# Очищаем ненужные файлы
RUN find /frontend-build/build -name "*.map" -delete

# ============= STAGE 2: Prepare Backend =============
FROM python:3.11-slim AS backend-builder

WORKDIR /backend-build

# Устанавливаем системные зависимости
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Копируем requirements и устанавливаем Python зависимости
COPY backend/requirements-monolith.txt ./
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements-monolith.txt

# Копируем backend код
COPY backend/app.py ./
COPY backend/shared/ ./shared/

# ============= STAGE 3: Production Runtime =============
FROM python:3.11-slim AS production

# Устанавливаем labels для метаданных
LABEL maintainer="DevAssist Pro Team"
LABEL version="1.0.0"
LABEL description="DevAssist Pro - Unified Fullstack Application"

# Устанавливаем системные зависимости
RUN apt-get update && apt-get install -y \
    nginx \
    supervisor \
    curl \
    procps \
    tzdata \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/* \
    && apt-get clean

# Создаем пользователей и группы
RUN groupadd -r devassist && useradd -r -g devassist devassist

# Создаем необходимые директории
RUN mkdir -p \
    /app \
    /app/data/uploads \
    /app/data/reports \
    /app/data/cache \
    /var/log/devassist \
    /var/log/nginx \
    /var/log/supervisor \
    /etc/supervisor/conf.d

# ===== BACKEND SETUP =====
WORKDIR /app

# Копируем Python зависимости из builder stage
COPY --from=backend-builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages
COPY --from=backend-builder /usr/local/bin /usr/local/bin

# Копируем backend код
COPY --from=backend-builder /backend-build/ ./

# ===== FRONTEND SETUP =====
# Удаляем дефолтную конфигурацию nginx
RUN rm -f /etc/nginx/sites-enabled/default /etc/nginx/sites-available/default

# Копируем собранный frontend
COPY --from=frontend-builder /frontend-build/build /usr/share/nginx/html

# Копируем nginx конфигурацию
COPY nginx.fullstack.conf /etc/nginx/nginx.conf

# Создаем кастомные error pages
RUN echo '<!DOCTYPE html><html><head><title>404 - Page Not Found</title><meta charset="utf-8"><style>body{font-family:Arial,sans-serif;text-align:center;padding:50px;background:#f5f5f5}h1{color:#333}p{color:#666}</style></head><body><h1>404 - Страница не найдена</h1><p>Запрашиваемая страница не существует.</p><a href="/">Вернуться на главную</a></body></html>' > /usr/share/nginx/html/404.html

RUN echo '<!DOCTYPE html><html><head><title>50x - Server Error</title><meta charset="utf-8"><style>body{font-family:Arial,sans-serif;text-align:center;padding:50px;background:#f5f5f5}h1{color:#d32f2f}p{color:#666}</style></head><body><h1>50x - Ошибка сервера</h1><p>Что-то пошло не так. Мы работаем над устранением проблемы.</p><a href="/">Попробовать снова</a></body></html>' > /usr/share/nginx/html/50x.html

# ===== SUPERVISOR CONFIGURATION =====
COPY <<EOF /etc/supervisor/conf.d/supervisord.conf
[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid
childlogdir=/var/log/supervisor

[program:backend]
command=python app.py
directory=/app
user=devassist
autostart=true
autorestart=true
stderr_logfile=/var/log/devassist/backend.err.log
stdout_logfile=/var/log/devassist/backend.out.log
environment=PYTHONUNBUFFERED="1"

[program:nginx]
command=nginx -g "daemon off;"
user=root
autostart=true
autorestart=true
stderr_logfile=/var/log/nginx/error.log
stdout_logfile=/var/log/nginx/access.log

[unix_http_server]
file=/var/run/supervisor.sock
chmod=0700

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface
EOF

# ===== STARTUP SCRIPT =====
COPY <<EOF /usr/local/bin/startup.sh
#!/bin/bash
set -e

echo "🚀 Starting DevAssist Pro Fullstack Application..."

# Проверяем переменные окружения
if [ -z "\$ANTHROPIC_API_KEY" ]; then
    echo "⚠️  Warning: ANTHROPIC_API_KEY not set"
fi

# Создаем директории с правильными правами
mkdir -p /app/data/{uploads,reports,cache}
chown -R devassist:devassist /app/data
chmod -R 755 /app/data

# Создаем log директории
mkdir -p /var/log/{devassist,nginx,supervisor}
chown -R devassist:devassist /var/log/devassist
chmod -R 755 /var/log

# Устанавливаем права на nginx файлы
chown -R www-data:www-data /usr/share/nginx/html
chmod -R 755 /usr/share/nginx/html

# Проверяем конфигурацию nginx
nginx -t

echo "✅ Configuration validated"
echo "📊 Available at: http://localhost:80"
echo "🏥 Health check: http://localhost:80/health"
echo "📚 API docs: http://localhost:80/api/docs"

# Запускаем supervisor
exec supervisord -c /etc/supervisor/conf.d/supervisord.conf
EOF

RUN chmod +x /usr/local/bin/startup.sh

# ===== HEALTH CHECK SCRIPT =====
COPY <<EOF /usr/local/bin/health-check.sh
#!/bin/bash
# Проверяем nginx
curl -f http://localhost:80/health || exit 1

# Проверяем backend
curl -f http://localhost:8000/health || exit 1

echo "All services healthy"
EOF

RUN chmod +x /usr/local/bin/health-check.sh

# ===== PERMISSIONS =====
RUN chown -R devassist:devassist /app \
    && chown -R www-data:www-data /usr/share/nginx/html \
    && chown -R devassist:devassist /var/log/devassist

# ===== ENVIRONMENT VARIABLES =====
ENV PYTHONUNBUFFERED=1
ENV PYTHONDONTWRITEBYTECODE=1
ENV NODE_ENV=production
ENV ENVIRONMENT=production

# ===== EXPOSE PORTS =====
EXPOSE 80

# ===== HEALTH CHECK =====
HEALTHCHECK --interval=30s --timeout=10s --start-period=20s --retries=3 \
    CMD /usr/local/bin/health-check.sh

# ===== VOLUMES =====
VOLUME ["/app/data"]

# ===== STARTUP =====
CMD ["/usr/local/bin/startup.sh"]